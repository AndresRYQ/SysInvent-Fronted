import { AlertTriangle, Check, X } from 'lucide-react'
import type { TipoDocumento } from '../../types/tipoDocumento'
interface Props { abierto: boolean; tipoDocumento: TipoDocumento | null; modo?: 'desactivar' | 'reactivar'; onClose: () => void; onConfirm: () => void }
export function TipoDocumentoDeleteModal({ abierto, tipoDocumento, modo = 'desactivar', onClose, onConfirm }: Props) { if (!abierto || !tipoDocumento) return null; const reactivar = modo === 'reactivar'; return <div className="maestro-modal-backdrop" role="presentation"><div className="maestro-modal-card maestro-modal-card--sm" role="dialog" aria-modal="true"><div className="maestro-delete-icon"><AlertTriangle size={24} /></div><h3 className="maestro-modal-title text-center">{reactivar ? 'Confirmar reactivación' : 'Confirmar desactivación'}</h3><p className="maestro-modal-copy text-center mb-0">{reactivar ? '¿Desea reactivar este registro?' : '¿Seguro que desea desactivar este registro?'}</p><p className="maestro-delete-name">{tipoDocumento.nombre}</p><div className="maestro-modal-footer maestro-modal-footer--center"><button type="button" className="btn btn-maestro-danger" onClick={onClose}><X size={18} />Cancelar</button><button type="button" className="btn btn-maestro-primary" onClick={onConfirm}><Check size={18} />{reactivar ? 'Reactivar' : 'Aceptar'}</button></div></div></div> }
