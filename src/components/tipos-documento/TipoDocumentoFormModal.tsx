import { useEffect, useState } from 'react'
import { Save, X } from 'lucide-react'
import type { TipoDocumento, TipoDocumentoFormData } from '../../types/tipoDocumento'
interface Props { abierto: boolean; tipoDocumento: TipoDocumento | null; error: string; soloLectura?: boolean; onClose: () => void; onSubmit: (datos: TipoDocumentoFormData) => void }
export function TipoDocumentoFormModal({ abierto, tipoDocumento, error, soloLectura = false, onClose, onSubmit }: Props) { const [form, setForm] = useState<TipoDocumentoFormData>({ nombre: '', descripcion: '' }); const [errores, setErrores] = useState({ nombre: '', descripcion: '' }); useEffect(() => { if (abierto) { setForm(tipoDocumento ? { nombre: tipoDocumento.nombre, descripcion: tipoDocumento.descripcion } : { nombre: '', descripcion: '' }); setErrores({ nombre: '', descripcion: '' }) } }, [abierto, tipoDocumento]); if (!abierto) return null; const cambiar = (campo: keyof TipoDocumentoFormData, valor: string) => { setForm((actual) => ({ ...actual, [campo]: valor })); setErrores((actual) => ({ ...actual, [campo]: '' })) }; const validar = () => { const e = { nombre: '', descripcion: '' }; const n = form.nombre.trim(); const d = form.descripcion.trim(); if (!n) e.nombre = 'Campo requerido'; else if (n.length < 2) e.nombre = 'Debe tener al menos 2 caracteres'; if (!d) e.descripcion = 'Campo requerido'; setErrores(e); return !e.nombre && !e.descripcion }; return <div className="maestro-modal-backdrop" role="presentation"><div className="maestro-modal-card" role="dialog" aria-modal="true"><div className="maestro-modal-header"><div className="maestro-modal-header__content"><h3 className="maestro-modal-title">{soloLectura ? 'Visualizar tipo de documento' : tipoDocumento ? 'Editar tipo de documento' : 'Registrar tipo de documento'}</h3></div><button type="button" className="btn maestro-modal-close" onClick={onClose} aria-label="Cerrar modal"><X size={18} /></button></div><form noValidate onSubmit={(e) => { e.preventDefault(); if (!soloLectura && validar()) onSubmit({ nombre: form.nombre.trim(), descripcion: form.descripcion.trim() }) }}><div className="maestro-modal-body">{error && <div className="alert alert-danger py-2">{error}</div>}<div className="mb-3"><label className="form-label maestro-label" htmlFor="tipoDocumentoNombreModal">Nombre del tipo de documento{!soloLectura && <span className="maestro-required">*</span>}</label><input id="tipoDocumentoNombreModal" className="form-control" value={form.nombre} placeholder="Ingresar" disabled={soloLectura} maxLength={80} onChange={(e) => cambiar('nombre', e.target.value)} />{errores.nombre && <div className="maestro-field-error">{errores.nombre}</div>}</div><label className="form-label maestro-label" htmlFor="tipoDocumentoDescripcionModal">Descripción{!soloLectura && <span className="maestro-required">*</span>}</label><textarea id="tipoDocumentoDescripcionModal" className="form-control" value={form.descripcion} placeholder="Ingresar" disabled={soloLectura} maxLength={250} rows={2} onChange={(e) => cambiar('descripcion', e.target.value)} /><div className="d-flex justify-content-between"><span className="maestro-field-error">{errores.descripcion}</span><small className="text-muted">{form.descripcion.length}/250</small></div></div>{!soloLectura && <div className="maestro-modal-footer"><button type="button" className="btn maestro-btn-danger" onClick={onClose}><X size={18} />Cancelar</button><button type="submit" className="btn maestro-btn-primary"><Save size={18} />{tipoDocumento ? 'Guardar cambios' : 'Registrar'}</button></div>}</form></div></div> }


