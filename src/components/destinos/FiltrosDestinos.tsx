import { Placeholder } from '../../constants/placeholders'
import { Filter, RotateCcw, Search } from 'lucide-react'
import { MaestroEstadoSelect } from '../common/MaestroEstadoSelect'
export interface FiltrosDestinosValores { nombre: string; estado: string }
interface Props { valores: FiltrosDestinosValores; onChange: (campo: keyof FiltrosDestinosValores, valor: string) => void; onBuscar: () => void; onLimpiar: () => void }
export function FiltrosDestinos({ valores, onChange, onBuscar, onLimpiar }: Props) { return <section className="maestro-filter-card card border-0 shadow-sm"><div className="card-body p-3 p-lg-3"><span className="maestro-kicker"><Filter size={16} />Filtros de búsqueda</span><div className="row g-3 mt-1"><div className="col-12 col-lg-8"><label className="form-label" htmlFor="nombreDestino">Nombre de destino</label><input id="nombreDestino" className="form-control" value={valores.nombre} placeholder={Placeholder.Buscar} onChange={(e) => onChange('nombre', e.target.value)} /></div><div className="col-12 col-md-6 col-lg-4"><label className="form-label" htmlFor="estadoDestino">Estado</label><MaestroEstadoSelect inputId="estadoDestino" value={valores.estado} onChange={(value) => onChange('estado', value)} /></div></div><div className="maestro-filter-actions mt-3"><button type="button" className="btn btn-maestro-secondary" onClick={onLimpiar}><RotateCcw size={18} />Limpiar</button><button type="button" className="btn btn-maestro-primary" onClick={onBuscar}><Search size={18} />Buscar</button></div></div></section> }
