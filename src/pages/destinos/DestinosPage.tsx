import { useEffect, useMemo, useState } from 'react'
import { FiltrosDestinos, type FiltrosDestinosValores } from '../../components/destinos/FiltrosDestinos'
import { TablaDestinos } from '../../components/destinos/TablaDestinos'
import { DestinoDeleteModal } from '../../components/destinos/DestinoDeleteModal'
import { DestinoFormModal } from '../../components/destinos/DestinoFormModal'
import { actualizarDestino, crearDestino, eliminarDestino, obtenerDestinos, reactivarDestino } from '../../services/destinoService'
import type { Destino, DestinoFormData } from '../../types/destino'
import '../../styles/DashboardPage.css'; import '../../styles/maestros.css'
const INICIALES: FiltrosDestinosValores = { nombre: '', estado: '' }
function filtrar(lista: Destino[], filtros: FiltrosDestinosValores) { const n = filtros.nombre.trim().toLowerCase(); return lista.filter((i) => (!n || i.nombre.toLowerCase().includes(n)) && (!filtros.estado || (filtros.estado === 'activo' ? i.activo === 1 : i.activo === 0))) }
export function DestinosPage() { const [lista, setLista] = useState<Destino[]>(() => obtenerDestinos()); const [filtros, setFiltros] = useState(INICIALES); const [aplicados, setAplicados] = useState(INICIALES); const [page, setPage] = useState(1); const [pageSize, setPageSize] = useState(10); const [formOpen, setFormOpen] = useState(false); const [soloLectura, setSoloLectura] = useState(false); const [seleccionado, setSeleccionado] = useState<Destino | null>(null); const [error, setError] = useState(''); const [confirmOpen, setConfirmOpen] = useState(false); const [reactivarOpen, setReactivarOpen] = useState(false); const [objetivo, setObjetivo] = useState<Destino | null>(null); const filtrados = useMemo(() => filtrar(lista, aplicados), [lista, aplicados]); const paginados = useMemo(() => filtrados.slice((page - 1) * pageSize, page * pageSize), [filtrados, page, pageSize]); useEffect(() => { setPage((p) => Math.min(p, Math.max(1, Math.ceil(filtrados.length / pageSize)))) }, [filtrados.length, pageSize]); const recargar = () => setLista(obtenerDestinos()); const cerrarForm = () => { setFormOpen(false); setSeleccionado(null); setSoloLectura(false); setError('') }; const guardar = (datos: DestinoFormData) => { try { seleccionado ? actualizarDestino(seleccionado.id, datos) : crearDestino(datos); recargar(); cerrarForm(); setPage(1) } catch (e) { setError(e instanceof Error ? e.message : 'Ocurrió un error inesperado.') } }; const cerrarConfirmacion = () => { setConfirmOpen(false); setReactivarOpen(false); setObjetivo(null) }; const confirmar = () => { if (!objetivo) return; try { confirmOpen ? eliminarDestino(objetivo.id) : reactivarDestino(objetivo.id); recargar() } finally { cerrarConfirmacion() } }; return <div className="destinos-page"><main className="dashboard-shell maestro-page-shell"><div className="container-xl px-0 maestro-page-body"><section className="maestro-topbar"><div className="maestro-topbar__copy"><h1>Destinos</h1><p>Administración de los destinos utilizados en las operaciones del almacén.</p></div></section><div className="maestro-panel"><FiltrosDestinos valores={filtros} onChange={(campo, valor) => setFiltros((a) => ({ ...a, [campo]: valor }))} onBuscar={() => { setAplicados({ ...filtros }); setPage(1) }} onLimpiar={() => { setFiltros(INICIALES); setAplicados(INICIALES); setPage(1) }} /></div><div className="maestro-panel"><TablaDestinos destinos={paginados} totalItems={filtrados.length} page={page} pageSize={pageSize} onAgregar={() => { setSeleccionado(null); setSoloLectura(false); setError(''); setFormOpen(true) }} onEditar={(i) => { setSeleccionado(i); setSoloLectura(false); setError(''); setFormOpen(true) }} onVisualizar={(i) => { setSeleccionado(i); setSoloLectura(true); setError(''); setFormOpen(true) }} onEliminar={(i) => { setObjetivo(i); setConfirmOpen(true) }} onReactivar={(i) => { setObjetivo(i); setReactivarOpen(true) }} onPageChange={setPage} onPageSizeChange={(s) => { setPageSize(s); setPage(1) }} /></div></div></main><DestinoFormModal abierto={formOpen} destino={seleccionado} error={error} soloLectura={soloLectura} onClose={cerrarForm} onSubmit={guardar} /><DestinoDeleteModal abierto={confirmOpen || reactivarOpen} destino={objetivo} modo={reactivarOpen ? 'reactivar' : 'desactivar'} onClose={cerrarConfirmacion} onConfirm={confirmar} /></div> }
